$ErrorActionPreference = "Stop"

throw @"
record_harmonic_playback.ps1 is disabled.

Use record_moveit_days_harmonic.sh through Ubuntu-22.04-ROS2 for acceptance
recordings. The old Windows screen-capture entry called play_harmonic_record.sh,
which has been removed because it used set-pose demo playback.
"@
